DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'local.db',
    }
}

# MongoDB Settings
MONGODB_HOST    = 'localhost'
MONGODB_PORT    = 27017
MONGODB_DBNAME  = 'dhlab'